= Terminal Calypso Crypto Legacy SAM API -- Specification
Calypso Networks Association <https://calypsonet.org>
v2.0.0-SNAPSHOT, {docdate}
:doctype: book
:toc: left
:toclevels: 4
:sectnums:
:sectnumlevels: 4
:sectanchors:
:sectlinks:
:data-uri:
:icons: font
:rouge-style: github
:source-highlighter: rouge
:docinfodir: {docdir}/assets
:docinfo: shared
:imagesoutdir: {docdir}/gen
:pdf-themesdir: {docdir}/assets
:pdf-theme: calypso
:pdf-fontsdir: {docdir}/assets/fonts
:experimental:
:xrefstyle: short
:figure-caption: Figure
:table-caption: Table
:example-caption: Example
:appendix-caption: Appendix
:organization: Calypso Networks Association
:short-name: CNA
:spec-name: Terminal Calypso Crypto Legacy SAM API
:spec-id: CNA-TCCL-API
:spec-version: 2.0.0-SNAPSHOT

[abstract]
== Abstract

This document is the official technical specification of the **{spec-name}** standardised
by the **{organization}** ({short-name}).
It defines, in a language-agnostic way, the interfaces, classes, enumerations, exceptions, structural
relationships and behavioural requirements that any conforming implementation of the {spec-name} MUST satisfy.

The {spec-name} exposes the abstractions required to operate Calypso *Legacy SAM* products (SAM C1, HSM C1, S1E1,
S1Dx). It is the symmetric counterpart of the *Terminal Calypso Crypto Symmetric API* ([CNA-TCCS-API]) for the
specific case where the crypto module is a legacy Calypso SAM.

[discrete]
== Document Status

[cols="1h,3", grid=rows, frame=all]
|===
| Specification ID    | {spec-id}
| Version             | {spec-version}
| Status              | Working Draft -- submitted to the CNA TC Terminal for validation
| Editor              | {organization}
| Reference license   | Eclipse Public License -- v 2.0 (EPL-2.0)
|===

NOTE: The present document specifies the {spec-version} of the {spec-name} as an *initial release*, without
historical reference to earlier drafts. It defines a single, coherent baseline against which conforming
implementations are evaluated.

// =============================================================================
== Introduction

=== Purpose

The {spec-name} defines the public surface used by terminal applications and card-transaction managers to:

* select, unlock and identify a Legacy SAM;
* read SAM parameters, system keys, work keys and event counters;
* write counter ceilings and configurations (with or without a control SAM);
* compute and verify *basic* and *traceable* signatures using SAM keys;
* perform PKI-related operations on the SAM: key-pair generation, card-certificate computation, lock-file
  transfer/write;
* operate *asynchronous* write transactions through a create/execute split;
* enrich a Calypso card transaction with SAM-backed crypto operations through a card-transaction extension.

The API spans three categories of transaction managers:

[cols="1,4", options="header"]
|===
| Manager family
| Purpose

| `FreeTransactionManager`
| Operations on the SAM without involving a *control SAM* -- the typical "read" workflow plus signature computation and verification.

| `SecureWriteTransactionManager`
| Synchronous *write* operations on the SAM, requiring a *control SAM* to authorise the key/lock transfers.

| `AsyncTransactionCreatorManager` / `AsyncTransactionExecutorManager`
| Two-step *asynchronous* write workflow: the creator prepares the command stream using a control SAM, the executor replays it on the target SAM.
|===

=== Scope

This specification covers:

* the SAM data model (`LegacySam`, `SamParameters`, `KeyParameter`);
* the selection extension used to enrich a Calypso reader selection with SAM-specific commands;
* the transaction managers, signature data carriers, key-pair and certificate computation data carriers;
* the security setting carrier and the card-transaction crypto extension;
* the SPIs implemented by the application to provide unlock data and revocation information;
* the exceptions raised by the transaction managers.

The following topics are *out of scope*:

* the on-the-wire APDU dialogue with the SAM (handled internally by implementations),
* the algorithms used by the SAM to derive keys or sign data,
* the storage of certificates and key parameters outside the SAM.

=== Conformance

A software product conforms to this specification if and only if:

. it implements every interface and class defined in <<sec_specification>> with the operations and semantics
  prescribed in this document,
. it raises exceptions exclusively of the types defined in <<sec_exceptions>> for the situations described,
. it preserves the structural and behavioural relationships described in <<sec_architecture>>.

Throughout this specification, the key words **MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, **RECOMMENDED**,
**MAY** and **OPTIONAL** are to be interpreted as described in **RFC 2119** and **RFC 8174** when, and only when,
they appear in all capitals.

In conformance with RFC 2119, **SHALL** is equivalent to **MUST**; this specification uses **MUST** exclusively in
order to avoid ambiguity.

=== Document Conventions

==== Language-agnostic types

[cols="1,3,3", options="header"]
|===
| Symbolic type   | Description                                                     | Typical bindings
| `string`        | Sequence of characters, UTF-8 encoded by default.                | `String`, `string`, `str`.
| `boolean`       | Two-state truth value.                                           | `boolean`, `bool`.
| `integer`       | Signed 32-bit integer.                                           | `int`, `Int32`.
| `byte`          | Signed 8-bit value.                                              | `byte`, `i8`.
| `byte array`    | Ordered sequence of octets.                                      | `byte[]`, `[u8]`, `bytes`.
| `date`          | Calendar date without a time-of-day or timezone.                 | `LocalDate`, `Date`.
| `sortedMap<K,V>`| Associative array sorted by key.                                 | `SortedMap<K,V>`, `BTreeMap<K,V>`.
| `void`          | Indicates that an operation returns no value.                    | `void`, `Unit`, `None`.
|===

=== References

[cols="1,1,4", options="header"]
|===
| Reference | Identifier | Title

| [[ref_rfc2119]] [RFC 2119]
| RFC 2119
| _Key words for use in RFCs to Indicate Requirement Levels_.

| [[ref_cna_tr]]  [CNA-TR-API]
| CNA-TR-API
| _Terminal Reader API Specification_, {organization}.

| [[ref_cna_tcc]] [CNA-TCC-API]
| CNA-TCC-API
| _Terminal Calypso Card API Specification_, {organization}.

| [[ref_cna_tccs]] [CNA-TCCS-API]
| CNA-TCCS-API
| _Terminal Calypso Crypto Symmetric API Specification_, {organization}.
|===

// =============================================================================
== Terms, Definitions and Abbreviations

=== Terms and Definitions

[cols="1,4", options="header"]
|===
| Term              | Definition

| SAM               | Secure Application Module -- a tamper-resistant cryptographic module used to perform Calypso operations.
| Legacy SAM        | A SAM compliant with the original Calypso SAM specifications (SAM C1, HSM C1, S1E1, S1Dx).
| Control SAM       | SAM used to authorise sensitive operations on a *target SAM* (e.g. key transfer).
| Target SAM        | SAM on which the operations are applied.
| System Key        | Key dedicated to the management of the SAM itself (personalisation, key management, reloading, authentication).
| Work Key          | Key used for application-level cryptographic operations.
| Event Counter     | Numeric counter managed by the SAM, typically used to track the usage of a work key.
| Counter Ceiling   | Maximum value an event counter is allowed to reach before being considered exhausted.
| Unlock Data       | Secret value required to unlock a locked SAM. Can be static or dynamic.
| Lock File         | SAM file controlling whether the SAM is locked or unlocked.
| SAM Traceability  | Mode in which the signature embeds the signing SAM serial number and the value of the counter associated with the signing key.
| Busy Mode         | Defensive mode that rate-limits repeated "PSO Verify Signature" attempts after a failure.
|===

=== Abbreviations

[cols="1,4", options="header"]
|===
| Abbreviation | Expansion

| APDU | Application Protocol Data Unit
| AID  | Application IDentifier
| CA   | Certification Authority
| CNA  | Calypso Networks Association
| HSM  | Hardware Security Module
| KIF  | Key Identifier
| KVC  | Key Version Code
| PKI  | Public Key Infrastructure
| PSO  | Perform Security Operation (ISO/IEC 7816-8 command class)
| SAM  | Secure Application Module
| SPI  | Service Provider Interface
| SV   | Stored Value
| UML  | Unified Modelling Language
|===

// =============================================================================
[[sec_architecture]]
== Architectural Overview

=== Functional positioning

[plantuml, target=architecture-overview, format=svg]
....
@startuml
skinparam shadowing false
skinparam roundCorner 8

component "Terminal Application" as App
component "Terminal Calypso Card API\n[CNA-TCC-API]" as TCC
component "Terminal Calypso Crypto\nSymmetric API [CNA-TCCS-API]" as TCCS
component "Terminal Calypso Crypto\nLegacy SAM API (this spec)" as TCCL
component "Legacy SAM\n(SAM C1, HSM C1, S1E1, S1Dx)" as SAM

App  --> TCCL : uses
App  --> TCC  : uses
TCCL ..> TCCS : implements (factory)
TCCL --> SAM  : drives
@enduml
....

=== Logical packages

[cols="1,1,4", options="header"]
|===
| Package                                              | Role     | Summary

| `calypso.crypto.legacysam`                           | Public API
| Factory, properties and shared enumerations (system key types, counter increment access, GetData tags).

| `calypso.crypto.legacysam.sam`                       | Public API
| SAM data model: `LegacySam`, `KeyParameter`, `SamParameters`, `LegacySamSelectionExtension`.

| `calypso.crypto.legacysam.spi`                       | SPI
| Application-implemented SPIs for unlock data computation and SAM revocation.

| `calypso.crypto.legacysam.transaction`               | Public API
| Transaction managers, signature/key/certificate data carriers, security setting, traceability mode and exceptions.
|===

[[sec_class_diagram]]
=== UML class diagram

The following UML class diagram provides a visual representation of the types and relationships defined by this specification:

[plantuml, target="api_class_diagram", format="svg"]
----
include::api_class_diagram.puml[Terminal Calypso Crypto Legacy SAM API class diagram, align=center]
----

// =============================================================================
[[sec_specification]]
== API Specification

=== Package calypso.crypto.legacysam

[[type_LegacySamApiProperties]]
==== LegacySamApiProperties

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Final class
| Package  | `calypso.crypto.legacysam`
| Since    | 0.1.0
| Purpose  | Exposes the immutable properties of the {spec-name}.
|===

===== Constants

[cols="1,1,1,4", options="header"]
|===
| Name      | Type     | Since | Description
| `VERSION` | `string` | 0.1.0 | String representation of the version of the API (e.g. `"2.0"`).
|===

[[type_LegacySamApiFactory]]
==== LegacySamApiFactory

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam`
| Since    | 0.3.0
| Purpose  | Factory used by the application to obtain instances of every public type provided by the API.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createLegacySamSelectionExtension() -> LegacySamSelectionExtension`
| Since       | 0.3.0
| Description | Returns a new <<type_LegacySamSelectionExtension,`LegacySamSelectionExtension`>>.
| Returns     | A non-`null` instance.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSymmetricCryptoCardTransactionManagerFactory(samReader: CardReader, sam: LegacySam) -> SymmetricCryptoCardTransactionManagerFactory`
| Since       | 0.3.0
| Description | Returns a new factory of [CNA-TCCS-API] `SymmetricCryptoCardTransactionManager` to be used to secure a Calypso card transaction with the supplied legacy SAM.
| Parameters  | `samReader` -- the reader through which the SAM is reached. +
                `sam` -- the associated control SAM.
| Returns     | A non-`null` factory.
| Throws      | `IllegalArgumentException` -- if any argument is `null` or invalid.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSecuritySetting() -> SecuritySetting`
| Since       | 0.3.0
| Description | Returns a new <<type_SecuritySetting,`SecuritySetting`>>.
| Returns     | A non-`null` instance.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createFreeTransactionManager(samReader: CardReader, sam: LegacySam) -> FreeTransactionManager`
| Since       | 0.3.0
| Description | Returns a new <<type_FreeTransactionManager,`FreeTransactionManager`>> bound to the supplied SAM and reader.
| Returns     | A `FreeTransactionManager`.
| Throws      | `IllegalArgumentException` -- if any argument is `null` or invalid.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSecureWriteTransactionManager(samReader: CardReader, sam: LegacySam, securitySetting: SecuritySetting) -> SecureWriteTransactionManager`
| Since       | 0.7.0
| Description | Returns a new <<type_SecureWriteTransactionManager,`SecureWriteTransactionManager`>> bound to the supplied SAM, reader and security setting.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | `IllegalArgumentException` -- if any argument is `null` or invalid.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createAsyncTransactionCreatorManager(targetSamContext: string, securitySetting: SecuritySetting) -> AsyncTransactionCreatorManager`
| Since       | 0.3.0
| Description
| Returns a new <<type_AsyncTransactionCreatorManager,`AsyncTransactionCreatorManager`>>. The target SAM context
  MUST have been produced by <<op_exportTargetSamContextForAsyncTransaction,`FreeTransactionManager.exportTargetSamContextForAsyncTransaction`>>.
| Throws      | `IllegalArgumentException` -- if any argument is `null` or invalid.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createAsyncTransactionExecutorManager(samReader: CardReader, sam: LegacySam, samCommands: string) -> AsyncTransactionExecutorManager`
| Since       | 0.3.0
| Description | Returns a new <<type_AsyncTransactionExecutorManager,`AsyncTransactionExecutorManager`>>. The `samCommands` MUST have been produced by an <<type_AsyncTransactionCreatorManager,`AsyncTransactionCreatorManager`>>.
| Returns     | A `AsyncTransactionExecutorManager`.
| Throws      | `IllegalArgumentException` -- if any argument is `null` or invalid.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createKeyPairContainer() -> KeyPairContainer`
| Since       | 0.5.0
| Description | Returns a new empty <<type_KeyPairContainer,`KeyPairContainer`>>.
| Returns     | A `KeyPairContainer`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createLegacyCardCertificateComputationData() -> LegacyCardCertificateComputationData`
| Since       | 0.5.0
| Description | Returns a new empty <<type_LegacyCardCertificateComputationData,`LegacyCardCertificateComputationData`>>.
| Returns     | A `LegacyCardCertificateComputationData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createBasicSignatureComputationData() -> BasicSignatureComputationData`
| Since       | 0.3.0
| Description | Returns a new empty <<type_BasicSignatureComputationData,`BasicSignatureComputationData`>>.
| Returns     | A `BasicSignatureComputationData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createTraceableSignatureComputationData() -> TraceableSignatureComputationData`
| Since       | 0.3.0
| Description | Returns a new empty <<type_TraceableSignatureComputationData,`TraceableSignatureComputationData`>>.
| Returns     | A `TraceableSignatureComputationData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createBasicSignatureVerificationData() -> BasicSignatureVerificationData`
| Description | Returns a new empty <<type_BasicSignatureVerificationData,`BasicSignatureVerificationData`>>.
| Returns     | A `BasicSignatureVerificationData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createTraceableSignatureVerificationData() -> TraceableSignatureVerificationData`
| Since       | 0.3.0
| Description | Returns a new empty <<type_TraceableSignatureVerificationData,`TraceableSignatureVerificationData`>>.
| Returns     | A `TraceableSignatureVerificationData`.
| Throws      | None.
|===

[[type_CounterIncrementAccess]]
==== CounterIncrementAccess

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Package  | `calypso.crypto.legacysam`
| Since    | 0.3.0
| Purpose  | Possible access rights for incrementing event counters using the *Increment Counter* command.
|===

[cols="1,1,4", options="header"]
|===
| Value                       | Since | Description
| `FREE_COUNTING_ENABLED`     | 0.3.0 | Enables the use of *Increment Counter* on the targeted event counter.
| `FREE_COUNTING_DISABLED`    | 0.3.0 | Forbids the use of *Increment Counter* on the targeted event counter.
|===

[[type_GetDataTag]]
==== GetDataTag

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Package  | `calypso.crypto.legacysam`
| Since    | 0.5.0
| Purpose  | Output data tags retrievable through the *Get Data* command. MAY NOT be applicable to all products.
|===

[cols="1,1,4", options="header"]
|===
| Value              | Since | Description
| `CA_CERTIFICATE`   | 0.5.0 | CA Certificate (CACert). MAY be unavailable.
|===

[[type_SystemKeyType]]
==== SystemKeyType

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Package  | `calypso.crypto.legacysam`
| Since    | 0.2.0
| Purpose  | System key types of a legacy SAM. Each type corresponds to a specific role.
|===

[cols="1,1,4", options="header"]
|===
| Value              | Since | Description
| `PERSONALIZATION`  | 0.2.0 | Personalisation key -- deciphers and authorises the writing of parameters and system keys.
| `KEY_MANAGEMENT`   | 0.2.0 | Work-file key -- deciphers and authorises the writing of work keys.
| `RELOADING`        | 0.2.0 | Reloading key -- deciphers and authorises the writing of counter ceilings.
| `AUTHENTICATION`   | 0.2.0 | Authentication key -- generates the signature of data read from the SAM.
|===

=== Package calypso.crypto.legacysam.sam

[[type_KeyParameter]]
==== KeyParameter

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.sam`
| Since    | 0.2.0
| Purpose  | Carries the parameters of a key managed by the SAM (system or work key).
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getRawData() -> byte array`
| Description | Returns the raw key parameter data: 13 bytes carrying KIF, KVC, algorithm and PAR1..PAR10.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKif() -> byte`
| Description | Returns the key identifier (KIF).
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKvc() -> byte`
| Description | Returns the key version (KVC).
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getAlgorithm() -> byte`
| Description | Returns the key algorithm identifier byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getParameterValue(parameterNumber: integer) -> byte`
| Description | Returns the value of the parameter whose number is supplied.
| Parameters  | `parameterNumber` -- the parameter number, in range `[1..10]`.
| Returns     | A `byte` value.
| Throws      | `IllegalArgumentException` -- if the argument is out of range.
|===

[[type_SamParameters]]
==== SamParameters

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.sam`
| Since    | 0.7.0
| Purpose  | Carries the parameters of the SAM as a single immutable record.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getRawData() -> byte array`
| Description | Returns the raw data of the SAM parameters as a 29-byte array.
| Returns     | A `byte array`.
| Throws      | None.
|===

[[type_LegacySam]]
==== LegacySam

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.sam`
| Extends  | `SmartCard` ([CNA-TR-API])
| Since    | 0.1.0
| Purpose  | Dynamic view of a legacy SAM, updated from selection to the end of the transaction.
|===

A `LegacySam` instance is obtained by casting the [CNA-TR-API] `SmartCard` produced by the selection scenario
when the selection extension was a <<type_LegacySamSelectionExtension,`LegacySamSelectionExtension`>>.

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getProductType() -> ProductType`
| Since       | 0.1.0
| Description | Returns the SAM product type. Possible values: `SAM_C1`, `HSM_C1`, `SAM_S1E1`, `SAM_S1DX`, `UNKNOWN`.
| Returns     | A `ProductType`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getProductInfo() -> string`
| Since       | 0.1.0
| Description | Returns a textual description of the SAM.
| Returns     | A `string`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSerialNumber() -> byte array`
| Since       | 0.1.0
| Description | Returns the SAM serial number.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getPlatform() -> byte`
| Since       | 0.1.0
| Description | Returns the platform identifier byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getApplicationType() -> byte`
| Since       | 0.1.0
| Description | Returns the application type byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getApplicationSubType() -> byte`
| Since       | 0.1.0
| Description | Returns the application subtype byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSoftwareIssuer() -> byte`
| Since       | 0.1.0
| Description | Returns the software issuer identifier.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSoftwareVersion() -> byte`
| Since       | 0.1.0
| Description | Returns the software version number.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSoftwareRevision() -> byte`
| Since       | 0.1.0
| Description | Returns the software revision number.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCounter(counterNumber: integer) -> integer`
| Since       | 0.1.0
| Description | Returns the value of the supplied counter (`[0..26]`). `null` if the value is not set.
| Returns     | An `integer` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCounters() -> sortedMap<integer, integer>`
| Since       | 0.1.0
| Description | Returns the values of the known counters, keyed by counter number.
| Returns     | A `sortedMap<integer, integer>`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCounterIncrementAccess(counterNumber: integer) -> CounterIncrementAccess`
| Since       | 0.2.0
| Description | Returns the increment-access mode for the supplied counter; `null` if unknown.
| Returns     | A `CounterIncrementAccess`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCounterCeiling(counterNumber: integer) -> integer`
| Since       | 0.1.0
| Description | Returns the ceiling value of the supplied counter; `null` if not set.
| Returns     | An `integer` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCounterCeilings() -> sortedMap<integer, integer>`
| Since       | 0.1.0
| Description | Returns the known ceiling values, keyed by ceiling number.
| Returns     | A `sortedMap<integer, integer>`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCaCertificate() -> byte array`
| Since       | 0.5.0
| Description | Returns the CA certificate retrieved from the SAM (384 bytes); `null` if not available.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSamParameters() -> SamParameters`
| Since       | 0.7.0
| Description | Returns the SAM parameters; `null` if not available.
| Returns     | A `SamParameters`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSystemKeyParameter(systemKeyType: SystemKeyType) -> KeyParameter`
| Since       | 0.2.0
| Description | Returns the parameters of the supplied system key; `null` if not available.
| Returns     | A `KeyParameter`.
| Throws      | `IllegalArgumentException` -- if the argument is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getWorkKeyParameter(recordNumber: integer) -> KeyParameter`
| Since       | 0.7.0
| Description | Returns the parameters of the work key at the supplied record number (`[1..126]`); `null` if not available.
| Returns     | A `KeyParameter`.
| Throws      | `IllegalArgumentException` -- if the record number is out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getWorkKeyParameter(kif: byte, kvc: byte) -> KeyParameter`
| Since       | 0.7.0
| Description | Returns the parameters of the work key referenced by its KIF/KVC pair; `null` if not available.
| Returns     | A `KeyParameter`.
| Throws      | None.
|===

===== Enumeration `ProductType`

Nested inside `LegacySam`.

[cols="1,4", options="header"]
|===
| Value       | Description
| `SAM_C1`    | SAM C1.
| `HSM_C1`    | SAM C1 HSM.
| `SAM_S1E1`  | SAM S1E1.
| `SAM_S1DX`  | SAM S1Dx.
| `UNKNOWN`   | Unidentified SAM.
|===

[[type_LegacySamSelectionExtension]]
==== LegacySamSelectionExtension

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.sam`
| Extends  | `CardSelectionExtension` ([CNA-TR-API])
| Since    | 0.3.0
| Purpose  | Enriches the [CNA-TR-API] selection scenario with SAM-specific commands (Unlock, Read Parameters, etc.).
|===

If the SAM is *locked*, three mutually exclusive unlocking strategies are offered:

. The application supplies directly the 16-byte unlock value expected by the SAM (static mode) -- see
  `setUnlockData`.
. The application supplies a <<type_LegacySamStaticUnlockDataProviderSpi,`LegacySamStaticUnlockDataProviderSpi`>>
  to compute the unlock value (possibly diversified with the SAM serial number).
. The application supplies a <<type_LegacySamDynamicUnlockDataProviderSpi,`LegacySamDynamicUnlockDataProviderSpi`>>
  to obtain the 8-byte value expected by the SAM in dynamic mode (computed by an origin SAM).

When the unlocking data is supplied by a provider, a [CNA-TR-API] `CardReader` is required for additional
exchanges. The reader MAY be provided either at selection-extension creation time, or later through the relevant
overload (e.g. dynamic SAM reader allocation).

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setUnlockData(unlockData: string) -> LegacySamSelectionExtension`
| Since       | 0.3.0
| Description | Sets the unlock data (8 or 16 bytes, supplied as a 32-character hexadecimal `string`) used to unlock a SAM C1 and schedules an *Unlock* command in the first position of the selection scenario. The *Unlock* command MUST be executed only after a successful filtering.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if the unlock data is `null`, malformed or out of range. + `IllegalStateException` -- if an unlocking setting has already been set.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setUnlockData(unlockData: string, productType: LegacySam.ProductType) -> LegacySamSelectionExtension`
| Since       | 0.3.0
| Description | Same as the previous overload, with an explicit target product type.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if any argument is `null`, malformed or out of range. + `IllegalStateException` -- if an unlocking setting has already been set.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setStaticUnlockDataProvider(staticUnlockDataProvider: LegacySamStaticUnlockDataProviderSpi) -> LegacySamSelectionExtension`
| Since       | 0.4.0
| Description | Sets the static-unlock-data provider. To be used when the SAM reader is supplied later in the workflow. The *Unlock* command is initiated after a successful filtering, followed by a request to the provider.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if the argument is `null`. + `IllegalStateException` -- if an unlocking setting has already been set.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setStaticUnlockDataProvider(staticUnlockDataProvider: LegacySamStaticUnlockDataProviderSpi, targetSamReader: CardReader) -> LegacySamSelectionExtension`
| Since       | 0.4.0
| Description | Same as the previous overload, with the target SAM reader supplied at creation time.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if any argument is `null`. + `IllegalStateException` -- if an unlocking setting has already been set.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setDynamicUnlockDataProvider(dynamicUnlockDataProvider: LegacySamDynamicUnlockDataProviderSpi) -> LegacySamSelectionExtension`
| Since       | 0.4.0
| Description | Sets the dynamic-unlock-data provider. The *Unlock* command is initiated after a successful filtering, followed by a request to the provider.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if the argument is `null`. + `IllegalStateException` -- if an unlocking setting has already been set.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setDynamicUnlockDataProvider(dynamicUnlockDataProvider: LegacySamDynamicUnlockDataProviderSpi, targetSamReader: CardReader) -> LegacySamSelectionExtension`
| Since       | 0.4.0
| Description | Same as the previous overload, with the target SAM reader supplied at creation time.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if any argument is `null`. + `IllegalStateException` -- if an unlocking setting has already been set.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadSamParameters() -> LegacySamSelectionExtension`
| Since       | 0.7.0
| Description | Schedules the execution of a *Read Parameters* command. The result MUST be available via `LegacySam.getSamParameters`.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadSystemKeyParameters(systemKeyType: SystemKeyType) -> LegacySamSelectionExtension`
| Since       | 0.3.0
| Description | Schedules the execution of a *Read Key Parameters* command for the supplied system key. The result MUST be available via `LegacySam.getSystemKeyParameter`.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if the argument is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadWorkKeyParameters(recordNumber: integer) -> LegacySamSelectionExtension`
| Since       | 0.7.0
| Description | Schedules the execution of a *Read Key Parameters* command for the work key at the supplied record number (`[1..126]`).
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if the record number is out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadWorkKeyParameters(kif: byte, kvc: byte) -> LegacySamSelectionExtension`
| Since       | 0.7.0
| Description | Schedules the execution of a *Read Key Parameters* command for the work key referenced by its KIF/KVC pair.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadCounterStatus(counterNumber: integer) -> LegacySamSelectionExtension`
| Since       | 0.3.0
| Description
| Schedules the execution of *Read Event Counter* and *Read Ceiling* commands to read the status of the supplied
  counter (`[0..26]`). Implementations MAY optimise the actual number of commands sent to the SAM by reading the
  whole record at once.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if the counter number is out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadAllCountersStatus() -> LegacySamSelectionExtension`
| Since       | 0.3.0
| Description | Schedules the reading of the status of every counter.
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareGetData(tag: GetDataTag) -> LegacySamSelectionExtension`
| Since       | 0.6.0
| Description | Schedules the execution of a *Get Data* command for the supplied tag. The result MUST be accessible via a dedicated getter (e.g. `LegacySam.getCaCertificate`).
| Returns     | A `LegacySamSelectionExtension`.
| Throws      | `IllegalArgumentException` -- if `tag` is `null`.
|===

=== Package calypso.crypto.legacysam.spi

[[type_LegacySamDynamicUnlockDataProviderSpi]]
==== LegacySamDynamicUnlockDataProviderSpi

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface (SPI)
| Package  | `calypso.crypto.legacysam.spi`
| Since    | 0.4.0
| Purpose  | Computes the dynamic unlock data expected by the SAM.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getUnlockData(samSerialNumber: byte array, samChallenge: byte array) -> byte array`
| Description
| Returns the dynamic unlock data computed by an origin SAM. The serial number and the challenge are required to
  prepare the *SAM Generate Unlock* command.
| Returns     | An 8-byte `byte array`.
| Throws      | None.
|===

[[type_LegacySamStaticUnlockDataProviderSpi]]
==== LegacySamStaticUnlockDataProviderSpi

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface (SPI)
| Package  | `calypso.crypto.legacysam.spi`
| Since    | 0.4.0
| Purpose  | Computes the static unlock data expected by the target SAM.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getUnlockData(samSerialNumber: byte array) -> byte array`
| Description | Returns the static unlock data expected by the target SAM. The serial number MAY be used as a diversifier.
| Returns     | A 16-byte `byte array`.
| Throws      | None.
|===

[[type_LegacySamRevocationServiceSpi]]
==== LegacySamRevocationServiceSpi

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface (SPI)
| Package  | `calypso.crypto.legacysam.spi`
| Since    | 0.1.0
| Purpose  | Checks dynamically whether a SAM is revoked.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isSamRevoked(serialNumber: byte array) -> boolean`
| Description | Checks if the SAM with the supplied serial number is revoked. The serial number MAY be complete (4 bytes) or partial (3 LSBytes).
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isSamRevoked(serialNumber: byte array, counterValue: integer) -> boolean`
| Description | Checks if the SAM with the supplied serial number and counter value is revoked.
| Returns     | A `boolean` value.
| Throws      | None.
|===

=== Package calypso.crypto.legacysam.transaction

[[type_SecuritySetting]]
==== SecuritySetting

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.2.0
| Purpose  | Security setting carrier for legacy-SAM transactions secured by a control SAM.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setControlSamResource(samReader: CardReader, controlSam: LegacySam) -> SecuritySetting`
| Description | Sets the control SAM and the reader through which it can be accessed.
| Returns     | A `SecuritySetting`.
| Throws      | `IllegalArgumentException` -- if any argument is `null`, or if the product type of `controlSam` is `UNKNOWN`.
|===

[[type_SamTraceabilityMode]]
==== SamTraceabilityMode

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.3.0
| Purpose  | SAM traceability mode used with traceable signature operations.
|===

[cols="1,1,4", options="header"]
|===
| Value                       | Since | Description
| `FULL_SERIAL_NUMBER`        | 0.3.0 | Full SAM serial number (4 bytes).
| `TRUNCATED_SERIAL_NUMBER`   | 0.3.0 | Truncated SAM serial number (3 LSBytes).
|===

[[type_TransactionManager]]
==== TransactionManager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | `CardTransactionManager` ([CNA-TR-API])
| Since    | 0.1.0
| Purpose  | Common base of every legacy-SAM transaction manager. Provides command processing for the SAM.
|===

The Legacy SAM `TransactionManager` is not generic; processing of prepared commands is inherited from
`CardTransactionManager` ([CNA-TR-API]) via the `processCommands()` operation. The Legacy SAM
`TransactionManager` does *not* redefine `processCommands()`; it relies on the inherited contract. Additional
failure modes specific to the SAM dialogue MAY be reported through `InvalidSignatureException` (signature
verification failure) and `InconsistentDataException` (data inconsistency).

[[type_ReadTransactionManager]]
==== ReadTransactionManager<T extends ReadTransactionManager<T>>

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | <<type_TransactionManager,`TransactionManager<T>`>>
| Since    | 0.1.0
| Purpose  | Adds *read*-oriented operations to the common transaction manager: SAM parameters, system/work keys and counter status.
|===

The operations on this interface mirror their equivalents on
<<type_LegacySamSelectionExtension,`LegacySamSelectionExtension`>>. `prepareReadSamParameters`,
`prepareReadSystemKeyParameters`, `prepareReadWorkKeyParameters` (both overloads), `prepareReadCounterStatus` and
`prepareReadAllCountersStatus` MUST behave identically.

[[type_FreeTransactionManager]]
==== FreeTransactionManager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | <<type_ReadTransactionManager,`ReadTransactionManager<FreeTransactionManager>`>>
| Since    | 0.1.0
| Purpose  | Transaction manager used when no control SAM is involved.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareGetData(tag: GetDataTag) -> FreeTransactionManager`
| Since       | 0.5.0
| Description | Schedules the execution of a *Get Data* command for the supplied tag.
| Returns     | A `FreeTransactionManager`.
| Throws      | `IllegalArgumentException` -- if `tag` is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareGenerateCardAsymmetricKeyPair(keyPairContainer: KeyPairContainer) -> FreeTransactionManager`
| Since       | 0.5.0
| Description | Schedules the execution of a *Card Generate Asymmetric Key Pair* command. The result MUST be written into the supplied container.
| Returns     | A `FreeTransactionManager`.
| Throws      | `IllegalArgumentException` -- if `keyPairContainer` is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareComputeCardCertificate(data: LegacyCardCertificateComputationData) -> FreeTransactionManager`
| Since       | 0.5.0
| Description | Schedules the execution of a *PSO Compute Certificate* command. The result MUST be written into the supplied data carrier.
| Returns     | A `FreeTransactionManager`.
| Throws      | `IllegalArgumentException` -- if `data` is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `preparePlainWriteLock(lockIndex: byte, lockParameters: byte, lockValue: byte array) -> FreeTransactionManager`
| Since       | 0.7.0
| Description | Schedules the execution of a *Write Key* command to set the lock file of the SAM. The lock value is transferred in plain text.
| Returns     | A `FreeTransactionManager`.
| Throws      | `IllegalArgumentException` -- if `lockValue` is `null` or out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareComputeSignature(data: SignatureComputationData<?>) -> FreeTransactionManager`
| Description
| Schedules the execution of a *Data Cipher* or *PSO Compute Signature* command. The signature MAY be used to
  certify card-resident data or terminal-emitted data. For the latter, the terminal SAM SHOULD carry a signing
  work key diversified with its own serial number.
| Returns     | A `FreeTransactionManager`.
| Throws      | `IllegalArgumentException` -- if the input data is inconsistent.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareVerifySignature(data: SignatureVerificationData<?>) -> FreeTransactionManager`
| Description | Schedules the execution of a *Data Cipher* or *PSO Verify Signature* command.
| Throws
| `IllegalArgumentException` -- if the input data is inconsistent. +
  `SamRevokedException` -- if the signature was computed in *SAM traceability* mode, the revocation check was
  requested and the SAM is revoked (traceable signatures only).
| Returns     | A `FreeTransactionManager`.
|===

[[op_exportTargetSamContextForAsyncTransaction]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `exportTargetSamContextForAsyncTransaction() -> string`
| Since       | 0.2.0
| Description
| Executes the commands required to obtain the security context of the target SAM. The returned `string` MUST be
  passed to <<type_LegacySamApiFactory,`LegacySamApiFactory.createAsyncTransactionCreatorManager`>>.
| Returns     | A non-empty `string`.
| Throws      | None.
|===

[[type_WriteTransactionManager]]
==== WriteTransactionManager<T extends WriteTransactionManager<T>>

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | <<type_TransactionManager,`TransactionManager<T>`>>
| Since    | 0.2.0
| Purpose  | Common base of every *write* transaction manager. Writes MAY be performed synchronously or asynchronously.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareWriteCounterCeiling(counterNumber: integer, ceilingValue: integer) -> T`
| Description
| Schedules a *Write Ceilings* command for a single counter. The ceiling value MUST be a positive integer less
  than or equal to `FFFFFAh`.

  *Warning:* in an asynchronous transaction, the content of the <<type_LegacySam,`LegacySam`>> object MUST NOT be
  updated.
| Returns     | A `T`.
| Throws      | `IllegalArgumentException` -- if any argument is out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature
| `prepareWriteCounterConfiguration(counterNumber: integer, ceilingValue: integer, counterIncrementAccess: CounterIncrementAccess) -> T`
| Description
| Schedules a *Write Ceilings* command for a counter ceiling and its free-incrementation configuration. Because
  the command writes a full record of nine counters, the application MUST have first read the status of the
  counters of that record (or have called this method for each of the other eight counters of the same record),
  otherwise the manager MUST raise an exception when processing the commands.
| Returns     | A `T`.
| Throws      | `IllegalArgumentException` -- if any argument is out of range.
|===

[[type_SecureWriteTransactionManager]]
==== SecureWriteTransactionManager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | <<type_WriteTransactionManager,`WriteTransactionManager<SecureWriteTransactionManager>`>>
| Since    | 0.7.0
| Purpose  | *Synchronous* write transaction manager that requires a control SAM. Used to write SAM parameters, transfer keys and transfer lock files.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareWriteSamParameters(parameters: byte array) -> SecureWriteTransactionManager`
| Description | Schedules a *Write Parameters* command. The supplied buffer MUST be 29 bytes long.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | `IllegalArgumentException` -- if the argument is `null` or out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareTransferSystemKey(systemKeyType: SystemKeyType, kvc: byte, systemKeyParameters: byte array) -> SecureWriteTransactionManager`
| Description | Schedules a *Write Key* command transferring a system key from the control SAM to the target SAM. `systemKeyParameters` MUST be 29 bytes long.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | `IllegalArgumentException` -- if any argument is `null` or out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareTransferSystemKeyDiversified(systemKeyType: SystemKeyType, kvc: byte, systemKeyParameters: byte array) -> SecureWriteTransactionManager`
| Description | Same as `prepareTransferSystemKey`, but the key is first diversified with the target SAM serial number.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareTransferWorkKey(kif: byte, kvc: byte, workKeyParameters: byte array, targetRecordNumber: integer) -> SecureWriteTransactionManager`
| Description
| Schedules a *Write Key* command transferring a work key from the control SAM to the target SAM. `workKeyParameters`
  MUST be 29 bytes long. `targetRecordNumber` MUST be in `[0..126]`; `0` means the SAM MUST choose the location.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | `IllegalArgumentException` -- if any argument is `null` or out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareTransferWorkKeyDiversified(kif: byte, kvc: byte, workKeyParameters: byte array, targetRecordNumber: integer) -> SecureWriteTransactionManager`
| Description | Same as `prepareTransferWorkKey`, with diversification by the target SAM serial number.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareTransferWorkKeyDiversified(kif: byte, kvc: byte, workKeyParameters: byte array, targetRecordNumber: integer, diversifier: byte array) -> SecureWriteTransactionManager`
| Description | Same as `prepareTransferWorkKey`, with diversification by the supplied 8-byte diversifier.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareTransferLock(lockIndex: byte, lockParameters: byte) -> SecureWriteTransactionManager`
| Description | Schedules a *Write Key* command transferring a lock file from the control SAM to the target SAM.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareTransferLockDiversified(lockIndex: byte, lockParameters: byte) -> SecureWriteTransactionManager`
| Description | Same as `prepareTransferLock`, with diversification by the target SAM serial number.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `preparePlainWriteLock(lockIndex: byte, lockParameters: byte, lockValue: byte array) -> SecureWriteTransactionManager`
| Description | Schedules a *Write Key* command to set the lock file with a plain-text 16-byte lock value.
| Returns     | A `SecureWriteTransactionManager`.
| Throws      | `IllegalArgumentException` -- if `lockValue` is `null` or out of range.
|===

[[type_AsyncTransactionCreatorManager]]
==== AsyncTransactionCreatorManager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | <<type_WriteTransactionManager,`WriteTransactionManager<AsyncTransactionCreatorManager>`>>
| Since    | 0.2.0
| Purpose  | Prepares write commands using a control SAM and exports them as a serialised stream for later replay by an <<type_AsyncTransactionExecutorManager,`AsyncTransactionExecutorManager`>>.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `exportCommands() -> string`
| Description | Returns a non-empty `string` carrying the prepared commands.
| Returns     | A `string`.
| Throws      | None.
|===

[[type_AsyncTransactionExecutorManager]]
==== AsyncTransactionExecutorManager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | <<type_TransactionManager,`TransactionManager<AsyncTransactionExecutorManager>`>>
| Since    | 0.2.0
| Purpose  | Executes a command stream prepared by an <<type_AsyncTransactionCreatorManager,`AsyncTransactionCreatorManager`>>. Declares no additional operation.
|===

[[type_SignatureComputationData]]
==== SignatureComputationData<T extends SignatureComputationData<T>>

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.1.0
| Purpose  | Common base contract for the input/output data of a signature computation.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setData(data: byte array, kif: byte, kvc: byte) -> T`
| Description | Sets the data to sign and the KIF/KVC of the key.
| Returns     | A `T`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setSignatureSize(size: integer) -> T`
| Description | Sets the expected signature size in bytes (`[1..8]`). The default size is 8 bytes; the longer the signature, the more secure it is.
| Returns     | A `T`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setKeyDiversifier(diversifier: byte array) -> T`
| Description | Sets an explicit 1-to-8-byte key diversifier. By default, the diversifier is the full serial number of the target card or SAM.
| Returns     | A `T`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSignature() -> byte array`
| Description | Returns the computed signature (1 to 8 bytes).
| Returns     | A `byte array`.
| Throws      | `IllegalStateException` -- if the command has not yet been processed.
|===

[[type_BasicSignatureComputationData]]
==== BasicSignatureComputationData

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | `SignatureComputationData<BasicSignatureComputationData>`
| Since    | 0.1.0
| Purpose  | Specialisation for basic signature computation using the *Data Cipher* command.
|===

`BasicSignatureComputationData` declares no additional operation.

[[type_TraceableSignatureComputationData]]
==== TraceableSignatureComputationData

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | `SignatureComputationData<TraceableSignatureComputationData>`
| Since    | 0.1.0
| Purpose  | Specialisation for traceable signature computation using the *PSO Compute Signature* command.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `withSamTraceabilityMode(offset: integer, samTraceabilityMode: SamTraceabilityMode) -> TraceableSignatureComputationData`
| Description
| Enables the *SAM traceability* mode. The SAM replaces the bits after the supplied offset by its serial number
  (3 or 4 bytes) followed by the new value (3 bytes) of the counter associated with the signing key. The mode is
  disabled by default.
| Returns     | A `TraceableSignatureComputationData`.
| Throws      | `IllegalArgumentException` -- if `samTraceabilityMode` is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `withoutBusyMode() -> TraceableSignatureComputationData`
| Description
| Disables the *Busy* mode. The Busy mode (enabled by default) rejects repeated *PSO Verify Signature* attempts
  for a few seconds after a failure. Implementations SHOULD recommend keeping the Busy mode enabled for
  security-sensitive flows.
| Returns     | A `TraceableSignatureComputationData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSignedData() -> byte array`
| Description | Returns the data that was actually signed. If *SAM traceability* mode was enabled, the returned data MUST embed the SAM traceability information.
| Returns     | A `byte array`.
| Throws      | `IllegalStateException` -- if the command has not yet been processed.
|===

[[type_SignatureVerificationData]]
==== SignatureVerificationData<T extends SignatureVerificationData<T>>

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.1.0
| Purpose  | Common base contract for the input/output data of a signature verification.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setData(data: byte array, signature: byte array, kif: byte, kvc: byte) -> T`
| Description | Sets the signed data, the signature and the KIF/KVC of the verification key.
| Returns     | A `T`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setKeyDiversifier(diversifier: byte array) -> T`
| Description | Sets an explicit 1-to-8-byte key diversifier. Default: full serial number of the target card or SAM.
| Returns     | A `T`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isSignatureValid() -> boolean`
| Description | Indicates whether the signature is valid.
| Returns     | A `boolean` value.
| Throws      | `IllegalStateException` -- if the command has not yet been processed.
|===

[[type_BasicSignatureVerificationData]]
==== BasicSignatureVerificationData

Specialisation of <<type_SignatureVerificationData,`SignatureVerificationData`>> for verifications performed with
the *Data Cipher* command. Declares no additional operation. _Since 0.1.0._

[[type_TraceableSignatureVerificationData]]
==== TraceableSignatureVerificationData

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | `SignatureVerificationData<TraceableSignatureVerificationData>`
| Since    | 0.1.0
| Purpose  | Specialisation for verifications performed with the *PSO Verify Signature* command.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature
| `withSamTraceabilityMode(offset: integer, samTraceabilityMode: SamTraceabilityMode, samRevocationService: LegacySamRevocationServiceSpi) -> TraceableSignatureVerificationData`
| Description
| Indicates that the signature was computed in *SAM traceability* mode. When `samRevocationService` is supplied,
  the revocation status of the signing SAM MUST be checked through it.
| Returns     | A `TraceableSignatureVerificationData`.
| Throws      | `IllegalArgumentException` -- if `samTraceabilityMode` is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `withoutBusyMode() -> TraceableSignatureVerificationData`
| Description
| Indicates that the signature was *not* computed in *Busy* mode. By default the signature is assumed to have been
  computed in Busy mode; in that case, if the verification fails with the *busy* status word, the application
  SHOULD retry until the busy window expires (a few seconds, never more than ten).
| Returns     | A `TraceableSignatureVerificationData`.
| Throws      | None.
|===

[[type_KeyPairContainer]]
==== KeyPairContainer

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.5.0
| Purpose  | Carries the output data of `prepareGenerateCardAsymmetricKeyPair`.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKeyPair() -> byte array`
| Description | Returns the generated key pair as a 96-byte `byte array`.
| Returns     | A `byte array`.
| Throws      | None.
|===

[[type_LegacyCardCertificateComputationData]]
==== LegacyCardCertificateComputationData

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.5.0
| Purpose  | Carries the input/output data of `prepareComputeCardCertificate`.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setCardPublicKey(cardPublicKey: byte array) -> LegacyCardCertificateComputationData`
| Description | Sets the card public key (64 bytes, *secp256r1* curve).
| Returns     | A `LegacyCardCertificateComputationData`.
| Throws      | `IllegalArgumentException` -- if the key is `null` or out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setStartDate(startDate: date) -> LegacyCardCertificateComputationData`
| Description | Sets the start of the validity period. Optional.
| Returns     | A `LegacyCardCertificateComputationData`.
| Throws      | `IllegalArgumentException` -- if the date is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setEndDate(endDate: date) -> LegacyCardCertificateComputationData`
| Description | Sets the end of the validity period. Optional.
| Returns     | A `LegacyCardCertificateComputationData`.
| Throws      | `IllegalArgumentException` -- if the date is `null`.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setCardAid(aid: byte array) -> LegacyCardCertificateComputationData`
| Description | Sets the AID of the autonomous PKI application of the target card. The AID MUST be 5 to 16 bytes and MUST NOT contain only zero bytes.
| Returns     | A `LegacyCardCertificateComputationData`.
| Throws      | `IllegalArgumentException` -- if the AID is `null`, out of range, or contains only zero bytes.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setCardSerialNumber(serialNumber: byte array) -> LegacyCardCertificateComputationData`
| Description | Sets the 8-byte card serial number.
| Returns     | A `LegacyCardCertificateComputationData`.
| Throws      | `IllegalArgumentException` -- if the argument is `null` or out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setCardStartupInfo(startupInfo: byte array) -> LegacyCardCertificateComputationData`
| Description | Sets the 7-byte startup info to embed in the certificate.
| Returns     | A `LegacyCardCertificateComputationData`.
| Throws      | `IllegalArgumentException` -- if the argument is `null` or out of range.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCertificate() -> byte array`
| Description | Returns the 316-byte certificate generated by the SAM, or `null` if the command has not been processed yet.
| Returns     | A `byte array`.
| Throws      | None.
|===

[[type_CardTransactionLegacySamExtension]]
==== CardTransactionLegacySamExtension

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Package  | `calypso.crypto.legacysam.transaction`
| Extends  | `CardTransactionCryptoExtension` ([CNA-TCC-API])
| Since    | 0.3.0
| Purpose
| Card-transaction crypto extension enriching the [CNA-TCC-API] command set with SAM-backed signature computation
  and verification. Instances are obtained through `SecureRegularModeTransactionManager.getCryptoExtension` or
  `SecureExtendedModeTransactionManager.getCryptoExtension`.
|===

===== Operations

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareComputeSignature(data: SignatureComputationData<?>) -> CardTransactionLegacySamExtension`
| Description | Schedules the execution of a *Data Cipher* or *PSO Compute Signature* command in the context of a Calypso card transaction.
| Returns     | A `CardTransactionLegacySamExtension`.
| Throws      | `IllegalArgumentException` -- if the input data is inconsistent.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareVerifySignature(data: SignatureVerificationData<?>) -> CardTransactionLegacySamExtension`
| Description | Schedules the execution of a *Data Cipher* or *PSO Verify Signature* command in the context of a Calypso card transaction.
| Throws
| `IllegalArgumentException` -- if the input data is inconsistent. +
  `SamRevokedException` -- if a revoked traceable signature was detected.
| Returns     | A `CardTransactionLegacySamExtension`.
|===

// =============================================================================
[[sec_exceptions]]
== Exceptions

[[type_InconsistentDataException]]
=== InconsistentDataException

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception (final)
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.1.0
| Purpose
| Indicates that inconsistent data have been detected -- for example a de-synchronisation between APDU requests
  and responses, or a discrepancy between data read inside and outside a session.
|===

[[type_InvalidSignatureException]]
=== InvalidSignatureException

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception (final)
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.1.0
| Purpose  | Indicates that a signature is invalid.
|===

[[type_SamRevokedException]]
=== SamRevokedException

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception (final)
| Package  | `calypso.crypto.legacysam.transaction`
| Since    | 0.1.0
| Purpose  | Indicates that the SAM is revoked.
|===

// =============================================================================
[[sec_behaviours]]
== Behavioural Requirements

. A `LegacySam` instance MUST be a *dynamic view* of the SAM image: every successful command processed by a
  synchronous transaction manager MUST update the relevant fields. Asynchronous write managers MUST NOT update
  the local `LegacySam`.
. The three unlocking strategies offered by `LegacySamSelectionExtension` MUST be mutually exclusive; an
  `IllegalStateException` MUST be raised if more than one is configured.
. Within a `SecureWriteTransactionManager`, every operation that targets a *target SAM* MUST involve a *control
  SAM* configured through <<type_SecuritySetting,`SecuritySetting`>>.
. The two-step asynchronous workflow (`AsyncTransactionCreatorManager` then `AsyncTransactionExecutorManager`)
  MUST preserve the order of the commands and MUST reject the execution if the exported context does not match
  the actual SAM image.
. The signature/verification managers MUST defer reading the signature or the validity flag until the commands
  have been processed; calling the corresponding getters before `processCommands()` MUST raise
  `IllegalStateException`.
. Communication errors at the reader, card or APDU level MUST be reported through the [CNA-TR-API] exceptions
  (`ReaderCommunicationException`, `CardCommunicationException`, `InvalidCardResponseException`); this
  specification does not duplicate them.

// =============================================================================
[appendix]
== Copyright and License

Copyright (c) {organization}, https://calypsonet.org.

This specification is distributed under the terms of the *Eclipse Public License -- v 2.0* (EPL-2.0). See https://www.eclipse.org/legal/epl-2.0/ for the license text.
